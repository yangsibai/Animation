{
  "name": "simple-animation",
  "version": "0.2.3",
  "description": "animation",
  "main": "dist/lib.js",
  "scripts": {
    "prepublish": "rm -rf dist; mkdir dist; gulp publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-live-server": "0.0.29"
  }
}